# Google OAuth Configuration
# Get these values from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Go to Credentials and create OAuth 2.0 Client ID
# 5. Add your domain to authorized origins

GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# Database Configuration (already set in docker-compose.yml)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=nasa
# POSTGRES_HOST=db
# POSTGRES_PORT=5432

# Application Configuration
# APP_PORT=8080

# Notification & Email Configuration
# SMTP details are required for sending AQI alerts
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USERNAME=notification-bot@your-domain.com
# SMTP_PASSWORD=super-secret-password
# SMTP_FROM=Clean Breathing <notification-bot@your-domain.com>
# AQI_BASE_URL=https://air-quality-api.open-meteo.com/v1/air-quality
# NOTIFICATION_INTERVAL_MIN=30

# Machine Learning Service Configuration
# ML_SERVICE_URL=http://localhost:8000
# ML_PREDICT_PATH=/predict

